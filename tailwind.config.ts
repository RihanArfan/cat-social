import type { Config } from "tailwindcss";

export default <Partial<Config>>{
  theme: {
    extend: {
      fontFamily: {
        cursive: ["Mansalva", "Mansalva override", "sans-serif"],
      },
    },
  },
};
